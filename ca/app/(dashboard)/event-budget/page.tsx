import { CalendarDays, Plus, WalletCards } from "lucide-react";
import { PageFrame, PageHeader } from "@/components/dashboard/page-header";
import { Button } from "@/components/ui/button";
import { formatCurrency } from "@/lib/utils";

const budgetLines = [{ name: "Venue & logistics", cap: 12000, spent: 10000, progress: "w-[83%]" }, { name: "Food & refreshments", cap: 15000, spent: 11900, progress: "w-[79%]" }, { name: "Prizes", cap: 10000, spent: 6500, progress: "w-[65%]" }, { name: "Marketing", cap: 5000, spent: 4200, progress: "w-[84%]" }];
export default function EventBudgetPage() { return <PageFrame><PageHeader title="Event budget" description="Keep every campus event on track, together." icon={WalletCards} action={<Button><Plus className="size-4" /> Add budget line</Button>} /><article className="surface max-w-4xl overflow-hidden"><div className="bg-gradient-to-r from-blue-700 to-emerald-600 p-6 text-white"><div className="flex justify-between"><div><p className="text-sm text-white/75">CodeFest ’26 budget</p><h2 className="mt-1 text-2xl font-semibold">{formatCurrency(42000)} allocated</h2></div><CalendarDays className="size-6" /></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-white/25"><div className="h-full w-[68%] rounded-full bg-white" /></div><p className="mt-2 text-xs text-white/75">68% utilized · ₹13,450 remaining</p></div><div className="p-4">{budgetLines.map((line) => <div className="flex items-center gap-4 border-b py-4 last:border-0" key={line.name}><div className="flex-1"><p className="text-sm font-medium">{line.name}</p><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-muted"><div className={`h-full rounded-full bg-emerald-500 ${line.progress}`} /></div></div><p className="text-sm font-semibold">{formatCurrency(line.spent)} <span className="text-xs font-normal text-muted-foreground">/ {formatCurrency(line.cap)}</span></p></div>)}</div></article></PageFrame>; }
