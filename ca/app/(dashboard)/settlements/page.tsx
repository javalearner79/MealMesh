import { ArrowRightLeft, CheckCircle2, Send } from "lucide-react";
import { PageFrame, PageHeader } from "@/components/dashboard/page-header";
import { Avatar } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
export default function SettlementsPage() { const items = [{ name: "Rohan Mehta", detail: "You owe for Hackathon supplies", value: "₹465", action: "Pay now" }, { name: "Aditi Kapoor", detail: "Owes you for groceries", value: "₹496", action: "Remind" }]; return <PageFrame><PageHeader title="Settlements" description="Clear balances without the group-chat arithmetic." icon={ArrowRightLeft} /><div className="grid max-w-4xl gap-5 md:grid-cols-2">{items.map(i => <article className="surface p-6" key={i.name}><div className="flex items-center gap-3"><Avatar name={i.name.split(" ").map(x => x[0]).join("")} /><div><h2 className="font-semibold">{i.name}</h2><p className="text-sm text-muted-foreground">{i.detail}</p></div></div><p className="mt-7 text-3xl font-semibold">{i.value}</p><Button className="mt-5 w-full"><Send className="size-4" /> {i.action}</Button></article>)}</div><article className="surface mt-6 max-w-4xl p-5"><div className="flex items-center gap-3"><CheckCircle2 className="size-5 text-emerald-500" /><div><h2 className="font-semibold">12 settlements completed</h2><p className="text-sm text-muted-foreground">You’ve kept your groups tidy this month.</p></div></div></article></PageFrame>; }
