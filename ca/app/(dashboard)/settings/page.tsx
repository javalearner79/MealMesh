import { Settings } from "lucide-react";
import { PageFrame, PageHeader } from "@/components/dashboard/page-header";
export default function SettingsPage() { return <PageFrame><PageHeader title="Settings" description="Fine tune your CampusSplit workspace." icon={Settings} /><section className="surface max-w-3xl divide-y">{[["Notifications", "Choose when we should nudge you about activity."], ["Payment methods", "Manage your UPI and bank preferences."], ["Privacy", "Control your profile and data visibility."], ["Appearance", "Theme follows your device by default."]].map(([title, copy]) => <button className="flex w-full items-center justify-between p-5 text-left hover:bg-muted/50" key={title}><div><p className="text-sm font-medium">{title}</p><p className="mt-1 text-sm text-muted-foreground">{copy}</p></div><span className="text-muted-foreground">›</span></button>)}</section></PageFrame>; }
