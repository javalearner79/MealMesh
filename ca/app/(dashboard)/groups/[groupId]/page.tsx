import Link from "next/link";
import { ArrowLeft, CheckCircle2, MoreHorizontal, Plus, ReceiptText, Send, Users } from "lucide-react";
import { Avatar } from "@/components/ui/avatar";
import { PageFrame } from "@/components/dashboard/page-header";
import { Button } from "@/components/ui/button";
import { expenses } from "@/constants/mock-data";
import { formatCurrency } from "@/lib/utils";
export default async function GroupDetailPage({ params }: { params: Promise<{ groupId: string }> }) { const { groupId } = await params; const name = groupId === "codefest-26" ? "CodeFest ’26" : groupId === "weekend-plans" ? "Weekend Plans" : groupId === "robotics-club" ? "Robotics Club" : "Hostel Block A"; return <PageFrame><Link className="inline-flex items-center gap-2 text-sm font-medium text-muted-foreground hover:text-foreground" href="/groups"><ArrowLeft className="size-4" /> All groups</Link><header className="mt-5 flex flex-wrap items-start justify-between gap-4"><div className="flex gap-4"><div className="grid size-14 place-items-center rounded-2xl bg-gradient-to-br from-blue-600 to-emerald-500 text-lg font-bold text-white">{name.split(" ").map((part) => part[0]).join("").slice(0, 2)}</div><div><h1 className="text-3xl font-semibold tracking-tight">{name}</h1><p className="mt-1 text-sm text-muted-foreground">12 members · Created 3 months ago</p></div></div><div className="flex gap-2"><Button variant="secondary"><MoreHorizontal className="size-4" /></Button><Button><Plus className="size-4" /> Add expense</Button></div></header><div className="mt-8 grid gap-6 xl:grid-cols-[1.55fr_.85fr]"><div className="space-y-6"><section className="grid gap-4 sm:grid-cols-3">{[["Total spent", "₹28,640", "text-foreground"], ["You are owed", "₹1,240", "text-emerald-600"], ["Outstanding", "₹465", "text-rose-500"]].map(([label, value, colour]) => <article className="surface p-5" key={label}><p className="text-sm text-muted-foreground">{label}</p><p className={`mt-2 text-2xl font-semibold ${colour}`}>{value}</p></article>)}</section><section className="surface"><div className="flex items-center justify-between border-b p-5"><div><h2 className="font-semibold">Group expenses</h2><p className="mt-1 text-sm text-muted-foreground">Everything added to this group.</p></div><Link className="text-sm font-medium text-primary" href="/expenses">View all</Link></div><div className="divide-y px-5">{expenses.slice(0, 3).map((expense) => <div className="flex items-center gap-3 py-4" key={expense.id}><div className="grid size-9 place-items-center rounded-xl bg-orange-50 text-orange-600 dark:bg-orange-950"><ReceiptText className="size-4" /></div><div className="flex-1"><p className="text-sm font-medium">{expense.title}</p><p className="text-xs text-muted-foreground">{expense.date} · Paid by {expense.paidBy}</p></div><p className="text-sm font-semibold">{formatCurrency(expense.amount)}</p></div>)}</div></section></div><aside className="space-y-6"><section className="surface p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Members</h2><p className="mt-1 text-sm text-muted-foreground">12 people in this group</p></div><Button variant="secondary" size="sm"><Users className="size-4" /> Invite</Button></div><div className="mt-5 space-y-4">{[["AK", "Arjun Kapoor", "You · Admin"], ["RM", "Rohan Mehta", "Owes ₹465"], ["PS", "Priya Shah", "All settled"], ["AS", "Aditi Kapoor", "Owes you ₹496"]].map(([initials, member, detail]) => <div className="flex items-center gap-3" key={member}><Avatar name={initials} /><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{member}</p><p className="text-xs text-muted-foreground">{detail}</p></div></div>)}</div><button className="mt-5 w-full text-sm font-medium text-primary">View all members</button></section><section className="surface p-5"><div className="flex gap-3"><div className="grid size-10 place-items-center rounded-xl bg-emerald-50 text-emerald-600 dark:bg-emerald-950"><CheckCircle2 className="size-5" /></div><div><h2 className="font-semibold">Simplify debts</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">There is one quick settlement available for this group.</p></div></div><Button className="mt-5 w-full"><Send className="size-4" /> Settle balances</Button></section></aside></div></PageFrame>; }
