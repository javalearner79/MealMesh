import { Bell } from "lucide-react";
import { PageFrame, PageHeader } from "@/components/dashboard/page-header";
import { Avatar } from "@/components/ui/avatar";
export default function NotificationsPage() { const list = [["Rohan added an expense", "Hackathon supplies was added to CodeFest '26", "12m"], ["Aditi settled up", "₹496 was paid for Monthly groceries", "2h"], ["Budget is almost full", "CodeFest '26 has used 68% of its budget", "1d"]]; return <PageFrame><PageHeader title="Notifications" description="The updates that matter to your groups." icon={Bell} /><section className="surface max-w-3xl divide-y">{list.map(([title, desc, age], i) => <article className="flex gap-4 p-5" key={title}><Avatar name={i === 0 ? "RM" : i === 1 ? "AK" : "CS"} /><div className="flex-1"><p className="text-sm font-medium">{title}</p><p className="mt-1 text-sm text-muted-foreground">{desc}</p></div><span className="text-xs text-muted-foreground">{age}</span></article>)}</section></PageFrame>; }
