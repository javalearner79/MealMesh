"use client";
import Link from "next/link";
import { AnimatePresence, motion } from "framer-motion";
import { Check, ChevronRight, FileImage, ImageUp, LoaderCircle, ReceiptText, ScanLine, ShieldCheck, Sparkles, UploadCloud, X } from "lucide-react";
import { ChangeEvent, DragEvent, useEffect, useRef, useState } from "react";
import { PageFrame, PageHeader } from "@/components/dashboard/page-header";
import { Button } from "@/components/ui/button";

type ScanState = "empty" | "scanning" | "complete";
const receiptItems = [["Whole wheat bread", "₹78"], ["Fresh vegetables", "₹246"], ["Milk and yogurt", "₹189"], ["Snacks & pantry", "₹167"]];

export default function ReceiptScannerPage() {
  const inputRef = useRef<HTMLInputElement>(null);
  const [state, setState] = useState<ScanState>("empty");
  const [fileName, setFileName] = useState("");
  const [dragging, setDragging] = useState(false);
  useEffect(() => { if (state !== "scanning") return; const timeout = window.setTimeout(() => setState("complete"), 1800); return () => window.clearTimeout(timeout); }, [state]);
  const beginScan = (file?: File) => { if (!file) return; setFileName(file.name); setState("scanning"); };
  const onInput = (event: ChangeEvent<HTMLInputElement>) => beginScan(event.target.files?.[0]);
  const onDrop = (event: DragEvent<HTMLDivElement>) => { event.preventDefault(); setDragging(false); beginScan(event.dataTransfer.files?.[0]); };
  const reset = () => { setFileName(""); setState("empty"); if (inputRef.current) inputRef.current.value = ""; };
  return <PageFrame><PageHeader title="Receipt scanner" description="Turn a receipt into a ready-to-split expense in seconds." icon={ScanLine} />
    <div className="grid max-w-6xl gap-6 xl:grid-cols-[minmax(0,1.05fr)_minmax(360px,.95fr)]"><section className="surface overflow-hidden"><div className="border-b p-5"><p className="flex items-center gap-2 text-sm font-semibold"><Sparkles className="size-4 text-emerald-500" /> AI-powered receipt capture</p><p className="mt-1 text-sm text-muted-foreground">Upload a receipt and we will extract the important details for you to review.</p></div><div className="p-5 sm:p-6"><AnimatePresence mode="wait">{state === "empty" ? <motion.div key="upload" initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -8 }} className={`grid min-h-[360px] place-items-center rounded-2xl border-2 border-dashed p-8 text-center transition-colors ${dragging ? "border-primary bg-blue-50/60 dark:bg-blue-950/30" : "border-border bg-muted/20"}`} onDragEnter={(event) => { event.preventDefault(); setDragging(true); }} onDragLeave={() => setDragging(false)} onDragOver={(event) => event.preventDefault()} onDrop={onDrop}><div><motion.div animate={{ y: dragging ? -5 : 0, scale: dragging ? 1.04 : 1 }} className="mx-auto grid size-16 place-items-center rounded-2xl bg-blue-50 text-primary shadow-sm dark:bg-blue-950"><UploadCloud className="size-7" /></motion.div><h2 className="mt-6 text-lg font-semibold">Drop your receipt here</h2><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-muted-foreground">Upload a photo or PDF. CampusSplit AI will identify the merchant, total, date, and line items.</p><input accept="image/jpeg,image/png,application/pdf" className="hidden" onChange={onInput} ref={inputRef} type="file" /><Button className="mt-6" onClick={() => inputRef.current?.click()} type="button"><ImageUp className="size-4" /> Choose receipt</Button><p className="mt-5 flex justify-center gap-2 text-xs text-emerald-600"><ShieldCheck className="size-3.5" /> JPG, PNG, and PDF · Up to 10 MB</p></div></motion.div> : <motion.div key="preview" initial={{ opacity: 0, scale: 0.98 }} animate={{ opacity: 1, scale: 1 }} className="relative grid min-h-[360px] place-items-center overflow-hidden rounded-2xl border bg-slate-100 p-6 dark:bg-slate-900"><div className="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-transparent via-emerald-400 to-transparent" />{state === "scanning" && <motion.div className="absolute inset-x-8 h-px bg-emerald-400 shadow-[0_0_16px_4px_rgb(52_211_153_/_0.45)]" initial={{ top: "18%" }} animate={{ top: "82%" }} transition={{ repeat: Infinity, repeatType: "reverse", duration: 1.2, ease: "easeInOut" }} />}<motion.div animate={state === "scanning" ? { rotate: [-1, 1, -1] } : { rotate: 0 }} transition={{ duration: 0.3 }} className="w-full max-w-[270px] rounded-lg bg-white p-5 text-slate-800 shadow-xl"><div className="flex items-center justify-between border-b border-dashed pb-3"><div><p className="text-sm font-bold">FreshMart</p><p className="text-[9px] text-slate-500">Campus Market · 22 Apr 2026</p></div><ReceiptText className="size-5 text-emerald-600" /></div><div className="space-y-2 border-b border-dashed py-4 text-[10px]">{receiptItems.map(([item, price]) => <div className="flex justify-between" key={item}><span>{item}</span><span>{price}</span></div>)}</div><div className="flex justify-between pt-3 text-sm font-bold"><span>TOTAL</span><span>₹680</span></div></motion.div><div className="absolute bottom-4 left-4 right-4 flex items-center justify-between rounded-xl border bg-background/90 p-3 text-xs backdrop-blur"><span className="flex min-w-0 items-center gap-2"><FileImage className="size-4 shrink-0 text-primary" /><span className="truncate">{fileName}</span></span><button className="rounded-md p-1 text-muted-foreground hover:bg-muted" onClick={reset} type="button"><X className="size-4" /></button></div></motion.div>}</AnimatePresence></div></section>
      <aside className="space-y-6"><AnimatePresence mode="wait">{state === "empty" ? <motion.section key="empty-help" initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="surface p-5"><h2 className="font-semibold">What AI extracts</h2><div className="mt-5 space-y-4">{[["Merchant", "FreshMart Campus Market"], ["Total amount", "₹680.00"], ["Date", "22 April, 2026"], ["Line items", "4 items detected"]].map(([label, detail]) => <div className="flex items-center justify-between" key={label}><span className="text-sm text-muted-foreground">{label}</span><span className="text-sm font-medium">{detail}</span></div>)}</div></motion.section> : state === "scanning" ? <motion.section key="scanning" initial={{ opacity: 0, x: 8 }} animate={{ opacity: 1, x: 0 }} className="surface p-6"><div className="grid size-12 place-items-center rounded-2xl bg-emerald-50 text-emerald-600 dark:bg-emerald-950"><LoaderCircle className="size-6 animate-spin" /></div><h2 className="mt-5 text-lg font-semibold">Reading your receipt</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Our AI is finding the merchant, total, date, and line items.</p><div className="mt-6 space-y-3">{["Checking image quality", "Reading receipt text", "Extracting line items"].map((step, index) => <div className="flex items-center gap-3 text-sm" key={step}>{index === 0 ? <Check className="size-4 text-emerald-500" /> : <span className="size-4 rounded-full border-2 border-emerald-500" />}{step}</div>)}</div></motion.section> : <motion.section key="result" initial={{ opacity: 0, x: 8 }} animate={{ opacity: 1, x: 0 }} className="surface overflow-hidden"><div className="border-b p-5"><p className="flex items-center gap-2 text-sm font-semibold text-emerald-600"><Check className="size-4" /> Receipt scanned successfully</p><h2 className="mt-2 text-lg font-semibold">Review extracted details</h2></div><div className="space-y-5 p-5"><div className="grid grid-cols-2 gap-3"><Result label="Merchant" value="FreshMart" /><Result label="Date" value="22 Apr 2026" /><Result label="Total" value="₹680.00" /><Result label="Confidence" value="98%" /></div><div><div className="flex items-center justify-between"><h3 className="text-sm font-semibold">Detected items</h3><span className="text-xs text-muted-foreground">4 items</span></div><div className="mt-3 divide-y rounded-xl border px-3">{receiptItems.map(([item, price]) => <div className="flex items-center justify-between py-2.5 text-sm" key={item}><span>{item}</span><span className="font-medium">{price}</span></div>)}</div></div><Link href="/expenses/add"><Button className="w-full">Create expense from receipt <ChevronRight className="size-4" /></Button></Link><button className="w-full text-sm font-medium text-muted-foreground hover:text-foreground" onClick={reset} type="button">Scan another receipt</button></div></motion.section>}</AnimatePresence><section className="rounded-2xl border border-blue-200 bg-blue-50/70 p-5 dark:border-blue-900 dark:bg-blue-950/30"><p className="flex items-center gap-2 text-sm font-semibold text-primary"><Sparkles className="size-4" /> Made for quick splits</p><p className="mt-2 text-sm leading-6 text-muted-foreground">You can edit every extracted field before adding the expense to a group.</p></section></aside>
    </div></PageFrame>;
}
function Result({ label, value }: { label: string; value: string }) { return <div className="rounded-xl bg-muted/60 p-3"><p className="text-[11px] text-muted-foreground">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></div>; }
