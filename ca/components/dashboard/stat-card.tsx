import { ArrowDownRight, ArrowUpRight, type LucideIcon } from "lucide-react";
import { cn } from "@/lib/utils";
export function StatCard({ label, value, change, icon: Icon, positive = true }: { label: string; value: string; change: string; icon: LucideIcon; positive?: boolean }) { return <article className="surface group p-5 transition-all hover:-translate-y-1 hover:shadow-lg"><div className="flex items-start justify-between"><div><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-semibold tracking-tight">{value}</p></div><div className="rounded-xl bg-blue-50 p-2.5 text-primary transition-transform group-hover:rotate-6 dark:bg-blue-950"><Icon className="size-5" /></div></div><p className={cn("mt-4 flex items-center gap-1 text-xs font-medium", positive ? "text-emerald-600" : "text-rose-500")}>{positive ? <ArrowUpRight className="size-3.5" /> : <ArrowDownRight className="size-3.5" />}{change} <span className="ml-1 text-muted-foreground">from last month</span></p></article>; }
