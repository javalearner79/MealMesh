"use client";
import { Bell, ChevronRight, Menu, Moon, Search, Sun } from "lucide-react";
import { useTheme } from "next-themes";
import { usePathname } from "next/navigation";
import { Avatar } from "@/components/ui/avatar";
import { useUIStore } from "@/store/ui-store";
export function Topbar() { const { setTheme, resolvedTheme } = useTheme(); const path = usePathname(); const setSidebarOpen = useUIStore((state) => state.setSidebarOpen); const title = path.split("/").filter(Boolean).at(-1)?.replaceAll("-", " ") || "Dashboard"; return <header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b bg-background/80 px-4 backdrop-blur-xl md:px-8"><div className="flex items-center gap-3"><button aria-label="Open navigation" onClick={() => setSidebarOpen(true)} className="rounded-lg p-2 hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary lg:hidden"><Menu aria-hidden="true" className="size-5" /></button><div className="hidden items-center gap-2 text-sm text-muted-foreground sm:flex"><span>Workspace</span><ChevronRight aria-hidden="true" className="size-4" /><span aria-current="page" className="capitalize text-foreground">{title}</span></div></div><div className="flex items-center gap-2"><label className="hidden h-10 w-64 items-center gap-2 rounded-xl border bg-card px-3 text-muted-foreground md:flex"><Search aria-hidden="true" className="size-4" /><input aria-label="Search workspace" placeholder="Search anything..." className="w-full bg-transparent text-sm placeholder:text-muted-foreground focus-visible:outline-none" /></label><button aria-label="Toggle colour theme" onClick={() => setTheme(resolvedTheme === "dark" ? "light" : "dark")} className="rounded-xl p-2.5 text-muted-foreground hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary">{resolvedTheme === "dark" ? <Sun aria-hidden="true" className="size-5" /> : <Moon aria-hidden="true" className="size-5" />}</button><button aria-label="View notifications" className="relative rounded-xl p-2.5 text-muted-foreground hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary"><Bell aria-hidden="true" className="size-5" /><span aria-hidden="true" className="absolute right-2 top-2 size-2 rounded-full bg-emerald-500 ring-2 ring-background" /></button><Avatar name="AK" /></div></header>; }
