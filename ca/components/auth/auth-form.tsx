"use client";
import Link from "next/link";
import { ArrowRight, Eye, EyeOff, LockKeyhole, Mail, UserRound } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { AuthBrand } from "./auth-shell";
import { FormField } from "@/components/ui/form-field";

export function AuthForm({ mode }: { mode: "login" | "signup" }) {
  const [showPassword, setShowPassword] = useState(false);
  const isLogin = mode === "login";
  return <section className="w-full max-w-[420px]"><div className="lg:hidden"><AuthBrand /></div><div className="mt-12 lg:mt-0"><p className="text-sm font-medium text-emerald-600">{isLogin ? "Welcome back" : "Start for free"}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{isLogin ? "Sign in to your workspace" : "Build better group money habits"}</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">{isLogin ? "Pick up where your group left off." : "Create your CampusSplit AI workspace in under a minute."}</p></div><form className="mt-8 space-y-4" onSubmit={(event) => event.preventDefault()}>{!isLogin && <FormField label="Full name" icon={UserRound}><input autoComplete="name" placeholder="Arjun Kapoor" /></FormField>}<FormField label="Email address" icon={Mail}><input autoComplete="email" placeholder="you@university.edu" type="email" /></FormField><FormField label="Password" icon={LockKeyhole}><input autoComplete={isLogin ? "current-password" : "new-password"} placeholder="Enter your password" type={showPassword ? "text" : "password"} /><button className="absolute right-3 top-1/2 -translate-y-1/2 rounded p-1 text-muted-foreground hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary" onClick={() => setShowPassword(!showPassword)} type="button" aria-label={showPassword ? "Hide password" : "Show password"}>{showPassword ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></FormField>{isLogin ? <div className="flex items-center justify-between"><label className="flex items-center gap-2 text-sm text-muted-foreground"><input className="size-4 rounded border" type="checkbox" /> Remember me</label><a className="text-sm font-medium text-primary hover:underline" href="#forgot-password">Forgot password?</a></div> : <label className="flex items-start gap-2 text-sm leading-5 text-muted-foreground"><input className="mt-0.5 size-4 rounded border" type="checkbox" /> I agree to the terms of service and privacy policy.</label>}<Link href="/dashboard" className="block"><Button className="mt-2 w-full">{isLogin ? "Sign in to CampusSplit" : "Create your account"} <ArrowRight className="size-4" /></Button></Link></form><div className="my-7 flex items-center gap-3"><span className="h-px flex-1 bg-border" /><span className="text-xs text-muted-foreground">OR CONTINUE WITH</span><span className="h-px flex-1 bg-border" /></div><button className="flex h-11 w-full items-center justify-center gap-2 rounded-xl border bg-card text-sm font-medium transition-colors hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary" type="button"><span className="grid size-5 place-items-center rounded-full bg-gradient-to-br from-red-500 via-yellow-400 to-blue-500 text-[9px] font-bold text-white">G</span> Google</button><p className="mt-7 text-center text-sm text-muted-foreground">{isLogin ? "New to CampusSplit AI?" : "Already have an account?"} <Link className="font-semibold text-primary hover:underline" href={isLogin ? "/signup" : "/login"}>{isLogin ? "Create an account" : "Sign in"}</Link></p></section>;
}
